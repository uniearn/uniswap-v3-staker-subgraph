{
  "name": "uniearn",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ uniearn/uniearn",
    "create-local": "graph create --node http://localhost:8020/ uniearn/uniearn",
    "remove-local": "graph remove --node http://localhost:8020/ uniearn/uniearn",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 uniearn/uniearn"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.1"
  }
}
